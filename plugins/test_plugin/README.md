# PluginSample.Plugin.TestPlugin

**TODO: Add description**

## Installation

If [available in Hex](https://hex.pm/docs/publish), the package can be installed as:

  1. Add `test_plugin` to your list of dependencies in `mix.exs`:

    ```elixir
    def deps do
      [{:test_plugin, "~> 0.1.0"}]
    end
    ```

  2. Ensure `test_plugin` is started before your application:

    ```elixir
    def application do
      [applications: [:test_plugin]]
    end
    ```

